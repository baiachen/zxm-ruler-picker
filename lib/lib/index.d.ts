import React from "react";
import "./index.css";
type IProps = {
    minScale: number;
    maxScale: number;
    initScale: number;
    onChange: any;
};
declare const Index: React.FC<IProps>;
export default Index;
